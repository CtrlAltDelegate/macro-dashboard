# Netlify: build runs from repo root; script writes landing into public/, then we publish that.
# If Base directory is set to "landing" in the Netlify UI, clear it (leave blank) so the command finds scripts/build_landing.py.
[build]
  base = "."
  command = "python scripts/build_landing.py"
  publish = "public"
