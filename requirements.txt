# Macro Dashboard — Private, local-first, newsletter-ready charts
pandas>=2.0.0
numpy>=1.24.0
fredapi>=0.5.0
yfinance>=0.2.0
plotly>=5.18.0
streamlit>=1.28.0
python-dotenv>=1.0.0
# Chart export: Kaleido first (pip only). Playwright optional if Kaleido fails.
kaleido>=0.2.1
playwright>=1.40.0
reportlab>=4.0.0
# AI interpretation (optional) — Anthropic Claude
anthropic>=0.40.0
# Macro Drivers news (RSS, no key)
feedparser>=6.0.0
requests>=2.28.0
